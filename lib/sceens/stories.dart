import 'package:flutter/material.dart';

class Stories extends StatelessWidget {
  final String listType;

  Stories(this.listType);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
